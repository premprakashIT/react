import React from 'react';
import ReactDOM from 'react-dom';
import Header from './component/Header'; // Assuming 'header' and 'footer' are components defined in separate files
import Footer from './component/Footer';

const App = () => {
  return (
    <div>
      <Header />
      <h1>Hii From React.</h1> {/* 'Hii' may be a typo, typically it's spelled 'Hi' */}
      <h2>This is my code</h2> {/* Corrected the sentence structure */}
      <Footer />
    </div>
  );
};

ReactDOM.render(<App />, document.getElementById('root'));
